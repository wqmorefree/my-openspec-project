## 1. 工程搭建与裁剪

- [ ] 1.1 拉取 RuoYi-Vue-Plus `fast` 分支代码到 `backend/`，锁定所使用提交号，验证 `mvn compile` 通过且版本号记录在案
- [ ] 1.2 移除 workflow（Flowable）、多租户相关模块与 demo 案例，验证 `mvn clean package -DskipTests` 通过且 `ruoyi-admin` 可独立启动
- [ ] 1.3 包名由 `org.dromara` 修改为 `com.openspec.project`，验证全库引用替换后 `mvn compile` 通过
- [ ] 1.4 修改 `pom.xml` 主版本与描述，验证聚合模块列表与裁剪后的目录结构一致

## 2. 前端工程引入 plus-ui

- [ ] 2.1 拉取 plus-ui 前端代码到 `frontend/`，锁定提交号，验证 `npm install` 与 `npm run dev` 正常启动
- [ ] 2.2 配置 `VITE_API_BASE_URL=http://localhost:8080/api`，验证前端可代理请求到后端
- [ ] 2.3 清理与项目无关的演示页面/图表/示例，验证打包 `npm run build` 通过

## 3. 数据库接入与迁移

- [ ] 3.1 配置业务数据源 `com.kingbase8.Driver` + `jdbc:kingbase8://localhost:54321/asset_db`，验证应用启动可连通金仓
- [ ] 3.2 配置 Flyway（`org.postgresql.Driver`）迁移目录，验证 `application-dev.yml` 的 flyway 配置生效且 `baseline-on-migrate` 正常
- [ ] 3.3 编写 V1 迁移脚本：创建 `find_in_set` 兼容函数 + 初始用户/角色/菜单基础数据，验证脚本在空库执行成功
- [ ] 3.4 MyBatis-Plus 分页方言设为 `POSTGRE_SQL`，验证分页查询返回正确 count 与数据

## 4. 认证与权限基线

- [ ] 4.1 启用 Sa-Token + JWT 登录认证，验证用户名/密码登录签发令牌、受保护接口校验通过
- [ ] 4.2 初始化预置数据：管理员、普通用户账号及两角色，验证两账号均可登录且权限集合不同
- [ ] 4.3 后端删除接口加 `@SaCheckPermission` 权限点，验证普通用户直调删除接口返回权限不足
- [ ] 4.4 前端登录页与用户管理页可用，验证管理员可新增用户、分配角色

## 5. 国密改造

- [ ] 5.1 实现 SM3 密码哈希工具（三次迭代 + 固定盐常量）并替换框架密码摘要，验证初始化用户与重置密码入库值均为 SM3 密文且登录校验通过
- [ ] 5.2 前端登录页引入 sm-crypto，密码经 SM2 加密后提交，验证后端 Hutool `SM2.decrypt` 还原后可正常登录
- [ ] 5.3 实现 `Sm4TypeHandler`（按 `database.md` 7.4：调用 kbcrypto `sm4()` 函数），验证同一明文加解密往返一致
- [ ] 5.4 为 sys_asset 敏感字段（组长手机、项目经理手机）接入 SM4 TypeHandler，验证数据库存密文、详情页解密展示

## 6. sys_asset 主表与 CRUD

- [ ] 6.1 使用代码生成器按 `sys_asset` 表产出基础 CRUD（Entity/Mapper/Service/Controller），验证生成后编译通过
- [ ] 6.2 系统编码唯一性：数据库唯一索引 + 后端保存前校验，验证重复编码新增被拒并返回"系统编码已存在"
- [ ] 6.3 系统编码不可修改：编辑接口忽略编码字段、前端只读，验证修改编码不生效
- [ ] 6.4 必填校验：编码/名称/业务域/负责人缺失时表单与后端双重拦截，验证保存被拒并提示缺失字段
- [ ] 6.5 删除仅管理员：前端 `v-hasPermi` 隐藏/禁用删除按钮 + 后端权限校验，验证普通用户两种途径均无法删除

## 7. 列表、搜索、筛选与导出导入

- [ ] 7.1 实现系统列表接口与页面（卡片/表格 + 分页 + 展示编码/名称/业务域/负责人/状态/创建时间），验证列表加载与翻页正常
- [ ] 7.2 关键词搜索（名称/编码模糊）、业务域与状态筛选、重置，验证三种条件组合过滤正确
- [ ] 7.3 状态字典配置（在建/已上线/已下线/维护中）并校验合法取值，验证非法值被拒
- [ ] 7.4 Excel 导入：EasyExcel 解析 + 必填/唯一校验（含文件内去重）+ 成功/失败明细返回，验证合法文件导入成功、重复编码提示明细
- [ ] 7.5 CSV 导出：按当前筛选导出并触发下载，验证导出结果与列表一致、空结果提示无数据

## 8. 详情页 Tab 宿主

- [ ] 8.1 实现详情路由 `/system/{id}` 与 12 个 Tab 导航，验证「基础信息」激活、其余 Tab 显示"建设中"占位
- [ ] 8.2 搭建子表组件注册表目录结构，验证后续 Tab 可通过注册挂载而框架零改动
- [ ] 8.3 基础信息 Tab 展示与编辑表单，验证详情数据加载、保存、敏感字段解密展示

## 9. 联调、测试与验收

- [ ] 9.1 数据权限与角色验证：管理员可删、普通用户禁删的端到端场景全部通过，对照 `specs/user-auth/spec.md` 各场景
- [ ] 9.2 基础信息全流程端到端：新增→列表→搜索筛选→详情→编辑→导出→批量导入，对照 `specs/system-registration/spec.md` 各场景
- [ ] 9.3 详情页 Tab 宿主行为符合 `specs/asset-registry-shell/spec.md`，验证未开放 Tab 提示且不破坏上下文
- [ ] 9.4 补充金仓专项测试（分页、find_in_set、大小写、TIMESTAMP），验证运行时无方言报错
- [ ] 9.5 全量 `mvn test` 通过并按 docs/rules/testing.md 附国密与合规测试证据